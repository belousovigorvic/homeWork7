import React from 'react';
import {Link} from "react-router-dom";

const StartScreen = () => {
    return (
        <div>
            <h1>Start Screen</h1>
        </div>
    );
};

export default StartScreen;